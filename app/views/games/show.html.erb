<p id="notice"><%= notice %></p>

<p>
  <strong>Name:</strong>
  <%= @game.name %><br>
  <strong>Season:</strong>
  <%= link_to @game.season.name, season_path(@game.season.id) %><br>
  <ul>
  <% @game.questions.each do |question| %>
    <li><%= link_to question.name, question_path %></li>
  <% end %>
  </ul>
</p>

<%= link_to 'Edit', edit_game_path(@game) %> |
<%= link_to 'Back', games_path %>
